/** Clean Lite stub — Armare 2D layer removed. */
export function RebarLayer(_props: {
  toSvg: (wx: number, wy: number) => { x: number; y: number };
  fromSvgEvent: (e: { clientX: number; clientY: number }) => { x: number; y: number };
  scale: number;
}) {
  return null;
}

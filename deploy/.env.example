# Copiază ca deploy/.env pe server
ALLOWED_ORIGINS=https://bbim.ciuntucbimstudio.ro

# Auth — set real values in deploy/.env (gitignored), never here.
JWT_SECRET=schimba-acest-secret-lung
ADMIN_USERNAME=schimba-acest-username
ADMIN_PASSWORD=schimba-aceasta-parola

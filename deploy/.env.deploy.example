# Opțional — pentru scriptul vechi setup-cloudflare-route.sh (tunel remote-managed).
# Pe serverul nou (YOUR_SERVER_IP) folosește preferabil setup-cloudflared.sh + tunel dedicat.
#
# Cloudflare Dashboard → My Profile → API Tokens → Create Token
# Permisiuni: Account → Cloudflare Tunnel → Edit, Zone → DNS → Edit

CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=31fd964ceb9ed68b9d5c840d05c3582
# Tunelul VECHI (ollama-hetzner) — nu mai folosi pentru bubblebim după mutare:
# CLOUDFLARE_TUNNEL_ID=284c6fe2-d571-4c7f-82d4-812b66d8d520
BUBBLEBIM_HOSTNAME=bubblebim.ciuntucbimstudio.ro
BUBBLEBIM_SERVICE=http://127.0.0.1:3458
